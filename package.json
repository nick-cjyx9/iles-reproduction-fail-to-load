{
  "name": "iles-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "now": "npm run build && npm run preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "pnpm": {
    "overrides": {
      "vue": "3.3",
      "vite": "4.4"
    }
  },
  "devDependencies": {
    "iles": "^0.9.5",
    "typescript": "^5.0",
    "vue-tsc": "^1.2"
  }
}
